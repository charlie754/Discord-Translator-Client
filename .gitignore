dist
node_modules
.VSCodeCounter
.vars
.secrets
.input

*.exe
vencord_installer
equicord_installer

.idea
.DS_Store

yarn.lock
bun.lock
package-lock.json

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
*.tsbuildinfo

.modules/
src/userplugins

ExtensionCache/
/settings

# vencord companion module cache
.modules

# wakatime related stuff
.wakatime-project

# Internal engineering record — turn reports, plans, review records.
# This repository is PUBLIC. These describe unfixed hazards, review findings and
# development history, and must not be published with the code. They live outside
# the repository; see the operator's local copy.
docs/
consultant/
